#ifndef FILESYSTEMS
#define FILESYSTEMS

enum {
    paretnDir = '\0';
    mountSystem = '\0';
    systemBlock = '\0';
}


typedef struct{
    space;
    
} SystemsMounting;
void mount(void);

#endif

